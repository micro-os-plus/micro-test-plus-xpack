# https://help.github.com/en/actions
# https://docs.github.com/en/actions/learn-github-actions/workflow-syntax-for-github-actions
# https://github.com/actions

name: CI on Push

on:
  push:

jobs:
  xpm-test:

    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        node-version: [14]
        os: [ubuntu-latest, macos-latest]

    steps:
    - name: Checkout
      uses: actions/checkout@v1
      with:
        fetch-depth: 3

    - name: Setup Node.js ${{ matrix.node-version }} on ${{ matrix.os }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}

    - name: Show environment
      run: |
        uname -a
        node --version
        npm --version
        env

    - name: Install xpm
      run: sudo npm install -g xpm@next

    - name: Satisfy dependencies
      run: npm install --all-configs

    - name: Run test-native-cmake
      run: xpm run test-native-cmake

    - name: Run test-native-cmake-gcc11
      run: xpm run test-native-cmake-gcc11
