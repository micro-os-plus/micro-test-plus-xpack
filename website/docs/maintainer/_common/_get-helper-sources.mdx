{/* DO NOT EDIT! */}
{/* Automatically generated from docusaurus-template-liquid/templates/docusaurus. */}

import CodeBlock from '@theme/CodeBlock';

{/* ------------------------------------------------------------------------ */}

## Get the writable helper sources

The project has a dependency to a common helper, that is normally installed
as a read-only dependency; for development purposes, to be able to make
changes to the scripts located inside the helper, clone the
xpack-development branch and link it to the user global xPacks store:

<CodeBlock language="sh"> {
`rm -rf ~/Work/micro-os-plus/${props.helperProjectName}.git && \\
mkdir -p ~/Work/micro-os-plus && \\
git clone \\
  --branch xpack-development \\
  https://github.com/micro-os-plus/${props.helperProjectName}.git \\
  ~/Work/micro-os-plus/${props.helperProjectName}.git \
`} </CodeBlock>

Or, if the repo was already cloned:

<CodeBlock language="sh"> {
`git -C ~/Work/micro-os-plus/${props.helperProjectName}.git pull
xpm link -C ~/Work/micro-os-plus/${props.helperProjectName}.git
`} </CodeBlock>
